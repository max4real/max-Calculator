import 'package:flutter/material.dart';

class ThemeModel {
  Color number;
  Color sign1;
  Color sign2;
  Color background;
  Color onBackground;
  Color numBackground;
  ThemeModel({
    required this.number,
    required this.sign1,
    required this.sign2,
    required this.background,
    required this.onBackground,
    required this.numBackground,
  });
}
